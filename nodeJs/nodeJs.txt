1.nodeJs适合开发什么？
    我们知道nodeJs善于I/O，不善于计算。因为nodejs擅长的是任务调度，如果业务上有很多的cpu计算，实际上也相当于这个计算阻塞了这个单线程，就不适合Node开发。

    当应用程序需要处理大量并发I/O，而在向客户端发出响应之前。应用程序内部并不需要进行非常复杂的处理，那么适合nodeJS。
    nodeJs非常适合与web socket配合，开发长连接额实时交互应用程序。